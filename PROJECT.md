# CUMCM 2026 A 题：药材烘干建模项目

> 本文档是项目级长期记忆。它记录当前有效的研究目标、模型假设、数值路线、运行方式、验收标准和关键决策；临时推导、完整结果表和调试过程应放在专题文档、代码或输出目录中。

## 1. 项目概况与当前状态

- 比赛：2026 年高教社杯全国大学生数学建模竞赛。
- 当前主线：暂定 A 题“药材的烘干问题”；C 题仅保留为发生重大物理或数值障碍时的回退方向，B 题不在当前工作范围内。
- 当前日期：2026-09-12（Asia/Shanghai）。
- 当前阶段：
  - 问题 1：已形成“输入校验—求解—数值验证—结构化输出—官方模板导出—论文图生成”的可重复运行代码。
  - 问题 2：模型推导完成；求解器已入库为 `src/q2_coupled.py`，可独立运行并已复现前 3 h 结果；`result2.xlsx` 尚未由程序导出。
  - 问题 3：求解完成并已独立复核，代码、方法文档与结果工作簿已入库；已知一处离散缺陷待修（见 §12 第 7 条）。
  - 问题 4：移动边界模型、Kirchhoff 界面离散、官方工作簿导出和论文图均已实现；正式连续终止时间为 **51.104 h**。
- 当前结论：问题 1 求解器在离散守恒、极限测试和网格收敛方面表现良好，`result1.xlsx` 可由程序自动重建。问题 1 只保留内部导热与表面对流换热模型，不再实现或计算潜热模型。问题 3 的烘干时间为 **57.5 h（≈2.40 天）**，由两套独立离散交叉确认（见 §10）。问题 4 在附件 4 物性和附件 2 收缩半径下为 **51.104 h（≈2.13 天）**；同物性的固定半径对照为 129.851 h，说明收缩是 Q4 加速干燥的主导因素。

## 2. 研究背景与物理分析目标

研究对象是一根近似圆柱形的药材。烘干过程中，热量由烘房空气经表面对流进入药材，水分在药材内部扩散并从表面进入空气。项目采用题面参数能够闭合的简化模型，目标是描述温度场和干基含水率场随时间、径向位置的变化，并形成可复现的数值结果和论文证据链。

四个问题的目标依次为：

1. **问题 1：短时常物性过程。** 计算前 1800 s 内固定圆柱的径向温度和含水率分布，建立常物性模型和数值验证基线。
2. **问题 2：固定尺寸变物性过程。** 使用附件 3 的经验物性，从原始初态重新计算完整热–湿耦合过程，并按要求输出前 3 h 的温度和含水率。
3. **问题 3：烘干终止时间。** 在固定尺寸模型下，求药材全域含水率均低于 \(0.15\ \mathrm{kg/kg}\) 的最早时刻。
4. **问题 4：收缩影响。** 根据附件 2 的半径变化建立移动边界模型，比较收缩对温度、水分迁移和烘干时间的影响。

## 3. 官方材料与数据

官方文件均位于 `data/official/A题/`，应视为只读输入，不得在原文件上修改。`data/official/` 同时保留其他题目的原始材料和比赛格式文件，作为选题档案。

- `A题.pdf`：题目正文。
- `附件/附件1.xlsx`：241 个时刻，时间范围 0–14400 s，间隔 60 s；包含烘房温度和烘房水分浓度，无缺失。
- `附件/附件2.xlsx`：145 个时刻，时间范围 0–259200 s，间隔 1800 s；药材半径由 2 cm 下降到约 1.198 cm，无缺失。
- `附件/附件3/result1.xlsx` 至 `result4.xlsx`：问题 1–4 的官方结果模板，必须由程序写入，避免手工复制。

当前数据解释：

- 附件 1 的离散边界数据在采样点之间采用分段线性插值。
- 问题 2 的结果只到 10800 s，处于附件 1 范围内，不需要外推。
- 问题 3、4 在 \(t>14400\ \mathrm s\) 时采用附件 1 平台段 \(t\ge9000\ \mathrm s\) 的样本均值作为恒温干燥边界：\(T_\infty=50.0049\ ^\circ\mathrm C\)、\(C_\infty=0.04999\ \mathrm{kg/kg}\)；附件末值作为敏感性对照，不作为主边界。取 \(t=9000\ \mathrm s\) 为平台起点是因为升温率已降到噪声量级；取均值而非末点是为避开单点噪声。问题 3 的末值延拓敏感性结果为 57.53 h 变为 57.25 h，差约 0.5%。
- 问题 4 的附件 2 半径数据在采样点间采用分段线性插值；若终止时间超过 72 h，则保持末值 \(R=1.198\ \mathrm{cm}\) 并明确报告外推。
- 烘房水分浓度和药材干基含水率虽然都以 kg/kg 表示，但分母物理含义不同；当前把前者视为可用于题面边界式的等效外部浓度，这是一项待校准的闭合假设。

### 3.1 官方优秀论文链接库

`literature/cumcm_official_papers/` 保存中国大学生在线“全国大学生数学建模竞赛论文展示”的本地可检索索引，正文仍指向官方页面，不复制论文文件或逐页图片。官网当前总入口覆盖 2012—2025 年；截至 2026-09-12，清理 3 个误列的年份入口并合并 10 个重复发布条目后，共有 140 篇逻辑论文：34 篇提供官方 PDF，90 篇以官方逐页图片展示，16 篇仅保留官方网页。更新入口为 `scripts/update_cumcm_paper_library.py`，输出包括 CSV、JSON、逐年 Markdown 和抓取报告。

该库只使用官方公开来源，不用第三方转载补全官网未展示的年份或缺失字段。论文展示页限制未经书面许可转载，因此项目不得把它改造成 PDF 镜像；需要阅读时应打开索引中的官方链接。

## 4. 当前基本假设

### 4.1 通用假设

1. 药材近似为长 \(L=0.25\ \mathrm m\)、初始半径 \(R=0.02\ \mathrm m\) 的圆柱。
2. 药材和环境关于中心轴对称，主要梯度沿径向；当前忽略轴向传热传质和两个端面的影响。
3. 药材视为均匀、各向同性的有效连续介质。
4. 内部传热采用傅里叶导热，内部水分迁移采用有效 Fick 扩散。
5. 圆柱中心采用零通量对称边界；表面采用第三类换热和传质边界。
6. 水分被等效为在药材内部迁移、在表面离开；题面给出的归一化含水率通量不直接解释为 kg/(m²·s) 的真实蒸发质量通量。
7. 问题 1–4 的正式模型均不考虑蒸发潜热，也不保留含潜热计算分支；后续问题继续通过含水率相关热物性和温度相关扩散系数实现热湿耦合。
8. 暂不考虑热辐射、化学反应热、内部宏观流动和多相局部非平衡。

### 4.2 问题 1

- 固定半径，不考虑收缩。
- \(\rho=820\ \mathrm{kg/m^3}\)、\(c_p=2600\ \mathrm{J/(kg\,K)}\)、\(k=0.36\ \mathrm{W/(m\,K)}\)。
- \(h=25\ \mathrm{W/(m^2\,K)}\)、\(h_m=8\times10^{-7}\ \mathrm{m/s}\)。
- \(T(r,0)=28\ ^\circ\mathrm C\)，\(C(r,0)=2.55\ \mathrm{kg/kg}\)。
- \(D(C)=7\times10^{-9}\exp(-0.89/C)\ \mathrm{m^2/s}\)。
- 温度场只考虑内部导热和表面对流换热，与水分场独立求解。

### 4.3 问题 2 与问题 3

- 问题 2 从 \(t=0\) 的原始初态重新求解，不先运行问题 1 再在 1800 s 切换物性。
- 整个计算时域统一采用附件 3 的 \(\rho(C)\)、\(c_p(C)\)、\(k(C)\) 和 \(D(C,T_K)\)，其中扩散系数中的温度必须使用 K。
- 问题 2、3 暂不考虑收缩；问题 4 才引入时变半径。
- 问题 2 不考虑蒸发潜热，不估算干物质体积密度，也不建立含潜热敏感性对照。
- 附录 3 未重申对流换热系数与传质系数，问题 2、3 沿用附录 2 的 \(h=25\ \mathrm{W/(m^2\,K)}\)、\(h_m=8\times10^{-7}\ \mathrm{m/s}\)；这是唯一可闭合的取法，论文中须写明这一沿用。
- 问题 3 的终止判据为全域判据 \(\max_r C(r,t)<0.15\ \mathrm{kg/kg}\)（题面为“低于”，取严格小于）。因剖面单调（中心最湿、表面最干），实际退化为中心含水率首次低于阈值，但实现中仍用 \(\max\) 而非 \(C(0,t)\)，以便剖面一旦非单调立即暴露。

### 4.4 问题 4

- 问题 4 从原始初态重新求解，整个时域统一采用附录 4 的 \(\rho(C)\)、\(c_p(C)\)、\(k(C)\) 和 \(D(C,T_K)\)。
- 圆柱长度保持不变，半径按附件 2 的 \(R(t)\) 变化；各材料层采用均匀径向相似收缩。
- 使用材料坐标 \(\xi=r/R(t)\) 将移动区域映射到 \([0,1]\)。物理坐标中的材料运动项与坐标移动项在该坐标中抵消，收缩通过 \(R^{-2}\) 的内部传输尺度和表面边界中的 \(R^{-1}\) 进入模型。
- 附录 4 的 \(\rho(C)\) 作为热方程的有效密度使用，不用于反推收缩率，也不直接乘入干基含水率方程。
- 问题 4 的物理终止判据继承问题 3，即全域 \(\max_\xi C(\xi,t)<0.15\ \mathrm{kg/kg}\)；为保证四位小数结果仍显示为严格低于 `0.1500`，工作簿最后时刻采用原始值 \(\max C<0.14995\) 的保守导出判据。
- `result4.xlsx` 使用固定物理距离 0–2 cm、间隔 0.1 cm 的列，并另设“药材表面”列；当固定距离超过当前 \(R(t)\) 时留空，不对药材外部位置外推。

## 5. 当前模型与关键接口

### 5.1 内部控制方程

温度采用轴对称圆柱中的守恒形式：

\[
\rho(C)c_p(C)\frac{\partial T}{\partial t}
=\frac1r\frac{\partial}{\partial r}
\left[rk(C)\frac{\partial T}{\partial r}\right].
\]

问题 1 中 \(\rho,c_p,k\) 为常数；问题 2、3 中它们随局部含水率变化。

水分采用

\[
\frac{\partial C}{\partial t}
=\frac1r\frac{\partial}{\partial r}
\left[rD(C,T_K)\frac{\partial C}{\partial r}\right].
\]

问题 1 中 \(D=D(C)\)，问题 2、3 中 \(D=D(C,T_K)\)。变系数 \(k\) 和 \(D\) 必须保留在散度算子内部。

### 5.2 表面第三类边界

题面形式的归一化传质边界为

\[
-D_s\left.\frac{\partial C}{\partial r}\right|_{r=R}
=h_m(C_s-C_\infty).
\]

该式两边单位均为 m/s，只定义以含水率变量表示的归一化通量。问题 1 不把它换算为额外的能量通量。温度采用题面给出的第三类换热边界：

\[
-k_s\left.\frac{\partial T}{\partial r}\right|_{r=R}
=h(T_s-T_\infty).
\]

### 5.3 耦合关系

- 问题 1：\(D\) 不依赖温度，热物性也为常数，因此正式温度场与水分场独立求解。
- 问题 2、3：温度通过 \(D(C,T_K)\) 影响水分，含水率通过 \(\rho(C),c_p(C),k(C)\) 影响温度，即使不加入潜热仍属于双向非线性耦合，必须同步或迭代求解。

## 6. 数值方法与实现原则

### 6.1 当前问题 1 求解器

`src/q1_fvm.py` 当前采用：

- 一维径向有限体积离散，中心和表面使用半控制体积；
- 界面扩散系数使用调和平均；
- Crank–Nicolson 型 \(\theta=0.5\) 时间推进，并用 Rannacher 启动抑制初始边界突变引起的振荡；
- 水分非线性方程使用 Picard 迭代；
- 分别求解温度场和水分场，只生成一套正式结果；
- 用恒定环境温度下的 Bessel 解析解检查热传导离散；
- 用局部/累计守恒残差和三组网格时间设置检查结果。
- 线性三对角方程使用内置 Thomas 分解，不再依赖 SciPy 稀疏求解器；SciPy 仅用于可选的 Bessel 解析基准。
- 对附件 1 的表头、数值完整性、时间单调性和时域覆盖进行运行前校验。
- 自动从官方模板副本生成 `outputs/q1/workbooks/result1.xlsx`，并回读检查两个工作表的尺寸、边界坐标和全部结果单元格。

当前基准网格为 \(N=400\)、\(\Delta t=0.5\ \mathrm s\)；收敛比较使用 \((N,\Delta t)=(200,1.0),(400,0.5),(800,0.25)\)。这些是当前实现参数，不是不可更改的物理设定。

### 6.2 后续问题

- 问题 2：继续采用守恒有限体积和隐式时间推进，在每个时间层内用 Picard 交替更新 \(C\)、\(D\)、\(\rho,c_p,k\) 和 \(T\)；不计算表面潜热通量。当前实现为顶点型网格（节点落在 0、0.1、…、2.0 cm，输出免插值）、界面 \(D\) 取调和平均、\(k\) 取算术平均、后向欧拉、三对角 Thomas 直接求解。
- 问题 3：复用问题 2 求解器并加终止判据，已实现于 `src/q3_drying_time.py`。相对问题 2 的三处实质调整：
  1. 长期边界按 §3 的规则常值延拓（93% 的积分区间落在附件 1 之外）；
  2. 分段变步长 1/5/20/60 s，取值与切换点均整除输出间隔 60 s，保证每个输出时刻精确落在步点上，无需时间插值；
  3. 网格加密到 \(N=800\)。**注意**：这项加密并非问题固有需要，而是界面调和平均在 \(D\) 退化区失准造成的，见 §12 第 7 条。
- 问题 4：在材料坐标 \(\xi=r/R(t)\) 上使用守恒有限体积法。先在物理坐标中写入材料速度 \(u=r\dot R/R\)，再利用相似收缩映射消去对流项；不得在固定域方程中重复加入网格运动项。
- 问题 4：水分扩散在内部界面和表面半网格采用 Kirchhoff 积分平均，导热系数可沿用普通界面平均；不得把点值调和平均在低含水率区造成的通量停滞解释为真实表面硬化。
- 问题 4 的水分守恒使用材料面积权重 \(2\int_0^1\xi C\,\mathrm d\xi\)，不能直接把随 \(R(t)^2\) 缩小的几何体积积分当成干基水分库存。
- 内部计算网格和时间步不得由结果模板的输出间隔反向决定；结果应在求解后插值或采样到官方网格。

## 7. 工作流程与当前优先级

1. **抽取公共模块。** 入库部分已完成（2026-09-12）：问题 2 求解器并入 `src/q2_coupled.py`，`src/q3_drying_time.py` 已改 import 并恢复可运行，57.53 h 已重新复现。剩余部分：数据读取、网格、算子组装、三对角求解和物性目前在 `q1_fvm.py` 与 `q2_coupled.py` 中各存一份，下一条的 Kirchhoff 修正会同时触及两处，应在动手前先抽成公共模块。
2. **修界面系数。** 把界面 \(D\) 的调和平均换成 Kirchhoff 积分平均（§12 第 7 条），重算问题 3 的网格收敛与 \(C_\infty\) 扫描，据此改写 `docs/q3/drying_time_method.md` 的方法 15 与方法 22。问题 4 编程前必须先做这一步，否则同一缺陷会带进移动边界模型。
3. **补问题 2、3 的正式输出。** `outputs/q2/data/` 的 .npy 已由求解器写出，工作簿仍缺：让 `result2.xlsx`、`result3.xlsx` 由程序从官方模板自动写入并回读验收，与问题 1 同规格。注意现有 `result3.xlsx` 并未按 §11.4 的保守判据生成——末行中心原始值 0.14999，四舍五入后显示为 `0.1500`，不满足“显示值严格低于 0.1500”的验收要求，重做导出时须落实 \(C<0.14995\)。
4. **问题 4 已实现。** `src/q4_moving_boundary.py` 已按材料坐标模型完成正式解、极限测试、收敛检查、固定半径对照、未舍入数据与 `result4.xlsx` 导出；Q4 从一开始即采用 Kirchhoff 界面，不继承 Q2/Q3 的调和平均缺陷。
5. **生成论文图。** 问题 1、2、3、4 均已有增强展示图入口；Q4 图直接读取未舍入材料坐标双场并映射回移动的物理区域。问题 2、3 的正式图仍须在其 Kirchhoff 修正和结果重算后再生。
6. **论文交付。** 固定图表和消融实验后撰写正文，确保每个关键数字都可由代码复现。

## 8. 当前文件与计划代码结构

### 8.1 当前有效文件

```text
PROJECT.md                                  # 项目级长期记忆
requirements.txt                           # 当前直接依赖，版本待验证后锁定
data/official/                              # 全部官方题目、附件和格式文件，只读
src/q1_fvm.py                               # 问题 1 完整求解、验证和导出入口
src/q1_visualize.py                         # 问题 1 论文图生成入口
src/visualization_style.py                  # 问题 1–4 共用论文图主题、语义色和轴样式
src/q2_coupled.py                           # 问题 2 求解入口，并为问题 3 提供物性、网格与离散算子
src/q2_visualize.py                         # 问题 2 增强展示图入口；正式图待 Kirchhoff 修正后重生
src/q3_drying_time.py                       # 问题 3 求解入口（物性与离散 import 自 q2_coupled）
src/q3_visualize.py                         # 问题 3 交付表预览图入口；正式数据源待切换为未舍入结果
src/q4_moving_boundary.py                   # 问题 4 材料坐标移动边界求解、验证与导出入口
src/q4_visualize.py                         # 问题 4 移动域、收缩机制与同物性对照图入口
scripts/update_cumcm_paper_library.py       # 更新官方优秀论文元数据与链接索引
literature/cumcm_official_papers/           # 2012—2025 官方优秀论文链接库（不镜像正文）
tests/test_cumcm_paper_library.py           # 论文库年份、PDF、题号和编号解析测试
tests/test_q1_fvm.py                        # 问题 1 快速回归与极限测试
docs/q1/heat_conduction.md                  # 问题 1 热传导
docs/q1/moisture_diffusion.md               # 问题 1 水分扩散
docs/q1/heat_equation_derivation.md         # 热方程补充推导
docs/q2/coupled_heat_moisture_derivation.md # 问题 2 推导源文档
docs/q3/drying_time_method.md               # 问题 3 方法详解（方法 15、22 待修正）
docs/q3/drying_time_independent_review.md   # 问题 3 独立复核报告
docs/q4/moving_boundary_heat_moisture_derivation.md # 问题 4 移动边界推导源文档
docs/reference_guide.md                      # 全项目参考文献选用、插入位置与引用边界指南
outputs/q1/data/                            # 问题 1 CSV、NPZ 和 JSON 数值结果与诊断
outputs/q1/workbooks/result1.xlsx           # 由官方模板自动生成的完整结果
outputs/q1/figures/                         # 问题 1 原始经典版论文图（保留，不由增强版覆盖）
outputs/q1/figures/fancy/                   # 问题 1 增强展示版 PNG、PDF、SVG
outputs/q2/data/                            # 问题 2 前 3 h 的温度、含水率和半径 .npy
outputs/q2/figures/fancy/                   # 问题 2 增强展示版 PNG、PDF、SVG
outputs/q3/data/                            # 问题 3 未舍入的 t、C、r .npy（2026-09-12 已生成）
outputs/q3/workbooks/result3.xlsx           # 问题 3 完整水分浓度结果
outputs/q3/figures/fancy/                   # 问题 3 增强展示版 PNG、PDF、SVG（当前为交付表预览图）
outputs/q4/data/                            # 问题 4 未舍入移动半径结果与固定半径对照缓存
outputs/q4/workbooks/result4.xlsx           # 问题 4 官方模板副本生成的完整含水率结果
outputs/q4/figures/fancy/                   # 问题 4 增强展示版 PNG、PDF、SVG
tmp/                                       # PDF 页面、缓存等临时文件
```

`__pycache__/`、`.DS_Store` 和 `tmp/` 中的内容不是研究成果。旧版草稿、独立潜热输出和过时的派生文档已从项目目录清理；当前可复现工作簿统一为 `outputs/q1/workbooks/result1.xlsx`。

### 8.2 目标结构

在继续开发时逐步整理为：

```text
src/        # 数据读取、物性、网格、模型和求解器
scripts/    # 问题 1–4 的一键运行入口
tests/      # 单元、解析解、守恒、极限和回归测试
outputs/    # 可由代码重建的结果、图表和工作簿
paper/      # 论文正文及定稿图表
logs/       # 参数、运行信息和收敛记录
```

不要为了目录整齐提前创建空结构；在问题 2 实现时再从 `src/q1_fvm.py` 抽取公共模块。

### 8.3 论文可视化规范

- 总体风格采用“论文印刷优先的现代科学可视化”：白底、克制留白、细网格和直接标注，以深蓝、青绿、橙红分别表示中心/内部、过渡状态和表面，环境边界使用灰色虚线，终止阈值使用黑色点线。高级感来自信息组织和关键机制显影，不采用缺乏信息增益的装饰性三维曲面、彩虹色图或深色大屏风格；三维双场地形仅作展示或附录补充。
- 四问形成连续叙事：问题 1 展示基本径向传输；问题 2 用同尺度对比突出变物性耦合；问题 3 用全域最大含水率、阈值线和终止区间放大说明烘干时长；问题 4 以叠加 $R(t)$ 边界的移动区域时空图为主视觉，并用“附录 4 固定半径—附录 4 移动半径”对照单独识别收缩效应。
- 正文优先保留约 6 张承担明确论证任务的核心图；网格收敛、边界敏感性、Kirchhoff/调和平均压力测试等放在数值验证或附录，不用重复热图堆叠篇幅。
- 所有图从未四舍五入的结构化结果读取，不从 Excel 交付表反向作图。比较图使用一致的坐标范围、色标、变量单位和系列配色；温度连续场采用低温浅色、高温深色的反向 `magma` 色标，含水率连续场采用低含水率浅色、高含水率深色的 `YlGnBu` 色标。物理坐标 $r$ 与材料坐标 $\xi$ 必须明确区分，移动边界外区域留白。
- 定稿图同时输出矢量 PDF/SVG 和 300 dpi PNG。论文图统一使用中文宋体（优先 `Songti SC`）、英文与数字 Times New Roman、数学公式 STIX；含中文的单位标签避免交给数学字体解析，采用 `kg·kg⁻¹` 等 Unicode 上标写法。统一字号、线宽、子图编号和图注。问题 2–4 应复用公共绘图主题与数据接口，特殊图形单独扩展，避免各问风格割裂。
- `src/q2_visualize.py` 从未四舍五入的 `Q2_T.npy`、`Q2_C.npy` 和 `Q2_r.npy` 读取前三小时结果，图形重点包括双场演化、变物性反馈和 Q1/Q2 同尺度比较。当前 Q2 最低含水率仍约为 1.008 kg/kg，尚未进入 Q3 的强退化低湿区，但正式图仍须在 Kirchhoff 修正后统一重生，不把现有调和平均结果当作最终离散版本。
- 问题 3 的正式图必须等待 Kirchhoff 界面修正和结果重算完成；不得把旧调和平均产生的“最优湿度”或“结壳停滞”绘制成论文结论。
- 在未四舍五入的 Q3 结构化结果入库前，`src/q3_visualize.py` 可只读现有 `result3.xlsx` 生成排版预览图，内容限于 57.5 h 主结果已被独立复核支持的演化趋势、空间梯度和达标先后次序。阈值图必须注明末行中心仅显示为 `0.1500`，不能由四位小数表单独证明严格不等式；这批图不得替代 Kirchhoff 修正后的正式数值证据。
- `src/q4_visualize.py` 读取新版、忽略大小写后仍唯一的 `Q4_time/Q4_Rhist/Q4_rout/Q4_full_Temp` 等 NPY 文件；不得读取旧的 `Q4_R/Q4_r` 或 `Q4_full_t/Q4_full_T` 组合，因为 macOS 默认大小写不敏感，旧命名会互相覆盖。Q4 默认生成总览、移动域双场、收缩传输机制、材料点历史、物理剖面、真实尺寸圆截面、固定/移动半径对照和几何—水分状态轨迹共 8 张图，每张均输出 PNG、PDF、SVG。

## 9. 运行流程

当前问题 1 主求解与 Excel 导出需要 Python、NumPy 和 OpenPyXL；SciPy 只用于恒温 Bessel 解析基准，缺失时会明确跳过该项而不影响主求解。`requirements.txt` 列出完整验证环境所需依赖，版本尚未锁定；应在竞赛使用环境验证通过后固定版本。

本机已确认一套可用解释器：`/Library/Frameworks/Python.framework/Versions/3.14/bin/python3`（NumPy 2.4.2、pandas 3.0.0、SciPy 1.17.0、openpyxl 3.1.5、matplotlib 3.10.8）。系统 `python3` 与 miniforge 默认环境均缺包，直接 `python` 运行会失败。问题 2、3 额外依赖 pandas 与 SciPy 的 `interp1d`，比问题 1 更重。

从项目根目录安装依赖并运行：

```bash
python -m pip install -r requirements.txt
python src/q1_fvm.py
```

程序默认会：

1. 读取 `data/official/A题/附件/附件1.xlsx`；
2. 运行恒温 Bessel 解析解对照；
3. 运行三组网格与时间步收敛检查；
4. 分别求解问题 1 的温度场和水分场；
5. 写出 `outputs/q1/data/q1_results_full.csv`、`q1_results_full.npz` 和 `q1_summary.json`；
6. 从官方模板副本生成 `outputs/q1/workbooks/result1.xlsx`，写入 1–1800 s、0–2 cm 每隔 0.1 cm 的完整结果并回读验收。

仅调试代码时可加 `--quick` 跳过网格加密和解析基准。快速回归测试命令为：

```bash
python -m unittest discover -s tests -v
```

生成问题 1 的默认增强版 PNG 图：

```bash
python src/q1_visualize.py
```

同时生成 PNG 和适合论文排版的 PDF：

```bash
python src/q1_visualize.py --formats png pdf
```

默认输出到 `outputs/q1/figures/fancy/`，包括四面板总览、温度/含水率时间曲线、双场时空热图、圆柱横截面演化图、温度-含水率状态轨迹、双场三维地形和不同时刻的径向剖面图；所有水分图统一显示实际干基含水率 \(C\)，不再使用下降量 \(\Delta C\)。`outputs/q1/figures/` 根目录中的经典版图不覆盖。时间曲线默认比较 \(r=0,0.5,1.0,1.5,2.0\ \mathrm{cm}\)，可通过 `--radii` 调整。

生成问题 2 的增强展示图：

```bash
/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 src/q2_visualize.py
```

默认读取 `outputs/q2/data/` 中的三个未舍入 NPY 文件，并在 `outputs/q2/figures/fancy/` 生成四面板总览、多半径双场历史、双场时空图、径向剖面、圆截面演化、温度—含水率耦合轨迹、变物性演化和 Q1/Q2 前 30 min 对比，各含 PNG、PDF、SVG 三种格式。脚本根据求解器约定重建 1–10800 s 时间轴，并补入已知的精确初态；Q1/Q2 对比从 Q1 的 NPZ 结构化结果读取。

生成问题 3 的交付表预览图：

```bash
python src/q3_visualize.py
```

默认从 `outputs/q3/workbooks/result3.xlsx` 只读加载 `Sheet1!A1:V3453`，并在 `outputs/q3/figures/fancy/` 独立生成总览、终止阈值及末段放大、多半径历史、含水率时空图、径向剖面、圆截面演化、达标前沿和中心—表面相图，各含 PNG、PDF、SVG 三种格式，不覆盖已有图。当前数据只有四位小数，图中明确保留该限制；正式定稿时应改读 Kirchhoff 修正求解器导出的未舍入结构化结果。

问题 2：

```bash
python src/q2_coupled.py
```

依次打印问题 1 常物性的退化回归、三组网格与时间步收敛、前 3 h 主求解与物理自检，写出 `outputs/q2/data/Q2_T.npy`、`Q2_C.npy`、`Q2_r.npy`，并打印论文表 3、表 4，全流程约 18 s。退化回归与问题 1 的偏差约 0.0006 °C 与 0.0001 kg/kg，来自后向欧拉与 Crank–Nicolson 的时间格式差异，不是模型差异。

问题 3：

```bash
python src/q3_drying_time.py
```

脚本依次打印边界延拓值、\(N=200/400/800\) 的网格收敛与 Richardson 外推、正式求解结果与物理自检，把 `Q3_t.npy`、`Q3_C.npy`、`Q3_r.npy` 写入 `outputs/q3/data/`，并打印论文表 5，全流程约 14 s。`result3.xlsx` 目前尚未由程序从官方模板自动写入，需按问题 1 的规格补上导出与回读验收，并落实 §11.4 的 \(C<0.14995\)。

问题 4 正式求解与导出：

```bash
/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 src/q4_moving_boundary.py --quick
```

快速模式完成移动半径正式解、物理自检、附录 4 固定半径对照、附录 3 固定半径复算、未舍入 NPY 与 `result4.xlsx` 导出；不运行网格收敛、零通量极限和边界延拓敏感性时可使用该模式，正式数值验收则去掉 `--quick`。结构化文件统一采用忽略大小写后仍不冲突的新命名。

生成问题 4 的增强展示图：

```bash
/Library/Frameworks/Python.framework/Versions/3.14/bin/python3 src/q4_visualize.py
```

默认输出到 `outputs/q4/figures/fancy/`。首次运行会用与正式 Q4 相同的 Kirchhoff 离散补算并缓存附录 4 固定半径对照；如求解器或参数发生变化，使用 `--recompute-control` 刷新该缓存。所有移动域图都由材料坐标解映射回物理半径，当前表面以外保持留白。

## 10. 当前结果及其解释边界

最近一次计算得到的主要结果和诊断为：

- 热方程累计相对守恒残差约 \(1.3\times10^{-14}\)；
- 水分累计相对守恒残差约 \(6.5\times10^{-14}\)；
- 1800 s 中心/表面温度约为 \(33.58/36.79\ ^\circ\mathrm C\)，与预热升温过程一致；
- 1800 s 中心/表面含水率约为 \(2.5500/1.5102\ \mathrm{kg/kg}\)。

代码结构调整后，旧版 `q1_results_full.csv`、`q1_results_full.npz` 和 `q1_summary.json` 已移出项目；重新运行程序后会按单一温度字段的新格式生成。

问题 3 的主要结果与诊断：

- **烘干时间 \(t_{\text{end}}=207120\ \mathrm s=57.53\ \mathrm h=2.40\) 天**，落在题面“2–3 天”的区间内。
- 终态 \(C\in[0.0527,\ 0.15]\)，中心恰好达标、表面最干；终态温度全场 50.0049 °C，内外温差 \(<10^{-4}\) °C。约 5–6 h 后即完全热平衡，此后耦合方程组自动退化为单个非线性扩散方程，可用该退化方程做低成本交叉校验。
- 传热与传质的特征时间相差 1–2 个量级：\(R^2/\alpha\approx0.5\sim0.8\ \mathrm h\)，\(R^2/D\approx8\sim139\ \mathrm h\)（随干燥增长 17 倍）。干燥速率从 6–12 h 段到 48–54 h 段衰减 70 倍，**不能用早期干燥曲线线性外推总时长**：按 6→12 h 速率外推会给出约 18 h，低估三倍以上。
- **独立复核（2026-09-12）**：用格心型有限体积 + SciPy BDF 自适应积分重算（空间离散、时间推进、终止时刻定位三者全部不同），得 \(N=800\) 时 57.467 h，收敛值 57.46–57.47 h；两套离散在 6–54 h 的内部各点偏差 \(\le5\times10^{-4}\ \mathrm{kg/kg}\)。当前实现的 57.533 h 相对收敛值偏高 0.07 h（0.12%），其自身 Richardson 外推 57.472 h 与独立结果重合。**论文报 57.5 h，不确定度 ±0.1 h**，并把这组交叉验证写进数值可信度一节。详见 `docs/q3/drying_time_independent_review.md`。

问题 4 的主要结果与诊断：

- 移动半径正式模型的连续终止时间为 \(183975\ \mathrm s=51.104\ \mathrm h=2.129\) 天；60 s 输出网格首次达标为 51.117 h，工作簿保守判据行是 51.133 h，末行最大显示值为 0.1499。
- 终止时半径约 1.200 cm，截面积约为初始值的 36.0%，内部传输尺度 \((R_0/R)^2\) 已放大到约 2.78 倍；终止时间早于附件 2 的 72 h 末端，因此本次答案不涉及半径外推。
- 在完全相同的附录 4 物性与 Kirchhoff 离散下，固定半径对照为 129.851 h，移动半径缩短 78.746 h，即 60.6%。附录 3 物性、固定半径的同离散复算为 57.487 h；因此不能把 Q3 与 Q4 的直接差值全部解释为收缩。
- 正式解的累计水分守恒闭合误差为 \(1.5\times10^{-14}\)，终态 \(C\in[0.05261,0.14995]\)，温度场已稳定在 50.0049 °C 附近。该守恒量验证组装正确性，不替代网格与时间收敛证据。

## 11. 验收标准

### 11.1 输入与复现

- 官方原始文件不被修改；生成文件全部进入独立输出目录。
- 数据点数、时间范围、单位和缺失值检查通过。
- 依赖、参数、运行入口和输出路径明确；全新环境能够重建论文使用的所有数字、图和工作簿。
- 内部计算不提前四舍五入，最终结果按题目要求保留位数。

### 11.2 物理与量纲

- 温度、含水率、热通量和归一化水分通量的单位一致。
- 中心热通量和水分通量为零；表面第三类边界正负号正确。
- 归一化水分库存变化与累计表面归一化流失量闭合。
- 正式热方程满足“储热项 = 累计对流输入”的离散平衡；变物性模型不得直接套用问题 1 的常物性总显热公式。
- 状态量不出现无物理依据的负值、过冲或振荡；附件 1 升温阶段的正式温度结果不应无依据地降到初温以下。

### 11.3 数值可信度

- 至少完成两组以上空间网格和时间精度的收敛比较。
- 常物性热方程通过 Bessel 解析解或另一独立方法交叉验证。
- 非线性迭代残差、局部守恒残差和累计守恒残差达到预定阈值。
- 极限检查通过：\(h_m=0\) 时归一化水分通量为零；\(h=h_m=0\) 时表面绝热且不失水。
- 正式结果检查换热/传质系数和关键边界解释。

### 11.4 各问交付

- 问题 1、2 的指定时间和径向位置结果稳定复现，并自动写入官方模板。
- 问题 3 使用全域判据 \(\max_r C(r,t)<0.15\)，终止时间对网格、时间精度和边界延拓规则均稳定，并与一套独立离散交叉确认。
- 交付工作簿采用原始值 \(C<0.14995\) 的保守终止条件，使四位小数显示后仍严格低于 `0.1500`；连续模型和论文中的物理阈值仍为 \(C<0.15\)。该导出处理会使终止行后移约 2–3 分钟，不改变小时尺度结论。
- 问题 4 明确空间坐标、材料坐标、表面位置和网格速度，移动边界下水分质量守恒；当 \(h_m=0\) 时，面积加权平均干基含水率必须在收缩过程中保持不变。
- 论文中的每个关键数字均能追溯到固定参数、代码版本和输出文件。

## 12. 主要风险与待决策事项

1. **界面含水率基准。** 固体干基含水率和空气含湿量不能天然直接作差；当前按题面形式把后者视为等效外部状态量，这是水分模型需要在论文中声明的主要简化。
2. **变物性能量解释。** \(\rho(C)c_p(C)\partial T/\partial t\) 是工程表观热容模型；若需要总焓模型，必须有额外物理依据，不能随意增加未知参数。
3. **长期环境边界。** 主模型已冻结为 4 h 后保持平台段均值 \(50.0049\ ^\circ\mathrm C\) 和 \(0.04999\ \mathrm{kg/kg}\)，但仍需用附件末值延拓进行终止时间敏感性检查。
4. **端面效应。** 当前一维径向简化需要用长径比、尺度分析或敏感性说明合理性。
5. **收缩模型。** 问题 4 已采用均匀径向相似收缩和材料坐标映射；附件只给出整体半径，无法识别局部非均匀应变、轴向收缩、开裂或表面硬化。
6. **工程复现。** 问题 1 已实现一键求解、结构化诊断和 Excel 导出，但依赖版本仍未锁定；Bessel 解析基准需要可选 SciPy，缺失时报告为跳过。问题 2 求解器已入库、问题 3 已恢复可运行，最大的可复现性缺口已闭合。剩余隐患是物性与离散代码在 `q1_fvm.py` 与 `q2_coupled.py` 中各存一份，任何离散修正都要同时改两处（见 §7 第 1 条）。
7. **界面系数在 \(D\) 退化区失准（Q2/Q3 旧实现待修，Q4 已规避）。** Q2/Q3 当前顶点型网格把节点放在 \(r=R\) 上，Robin 条件把该节点拉向 \(C_\infty\)，而 \(D=D_0e^{-0.45/C}\) 随之坍塌，界面调和平均 \(\approx2D_s\) 也归零，表面半控制体与内部**完全解耦**。Q4 从首版正式实现即使用 Kirchhoff 积分平均，没有继承这一缺陷。旧格式的后果有二：
   - \(C_\infty\lesssim0.04\) 时求解器给出“永远干不完”，且**加密网格不收敛**（\(N=200\) 与 \(N=800\) 的末态并不趋近）。`docs/q3/drying_time_method.md` 方法 22 据此写出的“表面硬化机制”“存在最优湿度 0.05”属数值假象，**不得写入论文**——表面硬化本身是真实工艺现象，但本模型并不预测停滞。
   - 方法 15 的“必须 \(N=800\)，否则时间误差 24%”同样由此产生，并非问题固有难度。

   正确做法是用 Kirchhoff 势 \(\Phi(C)=\int_0^C D\,\mathrm dc\)（有限且光滑，故真解的表面通量不会消失）构造界面系数 \(D_{\text{face}}=[\Phi(C_2)-\Phi(C_1)]/(C_2-C_1)\)，对一维稳态通量精确。实测改用后，顶点型格式在 \(N=200\) 即网格无关（\(N=800\) 给出完全相同的值），计算量降低约 16 倍。\(k\) 只跨 2.3 倍且不退化，仍用算术平均。
8. **低含水率区的模型适用范围。** 经验公式在 \(C<0.05\) 外推出 \(10^{-15}\) 乃至 \(10^{-28}\) 量级的扩散系数，已远超其标定范围；真实材料存在毛细、蒸汽扩散等并联机制，\(D\) 不会真正趋零。问题 3 的终态表面 \(C=0.0527\) 恰在这一边缘，论文应说明：若真实材料的 \(D\) 存在下限，实际烘干时间将短于本模型预测。
9. **质量守恒残差的解释边界。** 对守恒型有限体积 + 后向欧拉，逐单元方程求和后全局守恒是恒等式，\(10^{-19}\) 量级的残差验证的是组装与线性求解的正确性，不构成对离散误差或误差累积的检验，论文中不应作为精度证据。

## 13. 设计决策与已废弃方案

### 13.1 当前有效决策

- 2026-09-10：团队暂定 A 题为主线；C 题保留为重大障碍下的回退方案。
- 2026-09-11：问题 1 采用固定圆柱的一维轴对称径向模型，温度场与水分场独立求解。
- 2026-09-11：问题 2 从原始初态重新求解，并在整个时域使用附件 3 变物性；模型通过 \(D(C,T_K)\) 和含水率相关热物性形成双向耦合，不考虑表面蒸发潜热。
- 2026-09-11：问题 1、2 的正式温度模型不考虑潜热。（2026-09-12 补：问题 3、4 沿用同一口径，全项目统一无潜热。）
- 2026-09-11：空间离散优先使用守恒有限体积法，时间推进优先使用隐式方法；问题 1 原型采用 Crank–Nicolson、Rannacher 启动和 Picard 迭代。
- 2026-09-11：项目文件按 `data/`、`src/`、分题文档和分题输出重构；官方材料集中只读保存，源文档与派生展示文件分离，数值数据与工作簿分开归档。
- 2026-09-11：新增最小 `requirements.txt` 声明直接依赖；由于当前主机没有通过验证的完整 Python 环境，依赖版本暂不锁定，正式运行环境建立后再固定。
- 2026-09-11：问题 1 完成正式运行入口、官方 `result1.xlsx` 自动导出、回读验收和标准库单元测试；主三对角求解改为内置 Thomas 算法，SciPy 降为解析基准的可选依赖。
- 2026-09-11：根据量纲、变量基准和可识别性复核，将 `result1.xlsx` 导出确定为不含潜热的温度结果。
- 2026-09-12：冻结问题 2 的无潜热口径：能量方程和表面热边界均不含潜热项，不估算 \(\rho_d\)、\(L_v\) 或真实蒸发质量通量，也不建立含潜热对照模型。
- 2026-09-12：清理不再使用的独立潜热数据、旧潜热工作簿、旧原始草稿、过时 Q2 派生 HTML 和已完成使命的外部方案评审；保留 Q1 正式结果、综合诊断以及解释排除潜热所需的论证。
- 2026-09-12：进一步精简问题 1 求解器，删除潜热参数、通量接口、敏感性循环、模型选择参数及相关输出字段；问题 1 只生成一套温度结果。
- 2026-09-12：新增独立的 Q1 可视化入口，从 NPZ 结果生成位置时间曲线、双场时空热图和径向剖面图；默认输出 PNG，并支持 PDF、SVG。
- 2026-09-12：冻结问题 3、4 的长期烘房边界：附件 1 范围内分段线性插值，4 h 后采用 \(t\ge9000\ \mathrm s\) 平台段均值 \(T_\infty=50.0049\ ^\circ\mathrm C\)、\(C_\infty=0.04999\ \mathrm{kg/kg}\)，附件末值延拓只作敏感性对照。
- 2026-09-12：问题 4 采用附件 2 的分段线性半径、均匀径向相似收缩和材料坐标 \(\xi=r/R(t)\)。材料运动在固定材料坐标中不再作为额外对流项重复加入，水分守恒改用材料面积权重。
- 2026-09-12：问题 4 正式模型继续不考虑蒸发潜热，并沿用问题 1 的 \(h\) 和 \(h_m\)；附录 4 的密度只进入表观热容。
- 2026-09-12：为单独识别收缩效应，问题 4 除正式移动半径模型外，还需计算“附录 4 物性 + 固定初始半径”对照；问题 3 与问题 4 的直接差异不能全部归因于收缩。
- 2026-09-12：长期边界的常值在代码中实现为 \(t\ge9000\ \mathrm s\) 的平台均值 50.0049 °C / 0.04999 kg/kg（即上条冻结值的未舍入形式）；平台起点由附件 1 的升温率降至噪声量级确定，不是人为假设。
- 2026-09-12：问题 3 完成求解，答案 57.5 h（≈2.40 天）。代码、方法文档与结果工作簿按项目惯例入库为 `src/q3_drying_time.py`、`docs/q3/drying_time_method.md`、`outputs/q3/workbooks/result3.xlsx`；并入时只改文件路径，离散格式、步长调度与终止判据未动。
- 2026-09-12：完成问题 3 的独立复核（格心型 FVM + BDF 自适应积分，与现有实现完全独立的离散）。结论：烘干时间答案成立并给出 ±0.1 h 的不确定度；但**界面调和平均在 \(D\) 退化区失准**，方法详解中方法 22 的“表面硬化导致干不完”与方法 15 的“必须 \(N=800\)”为数值假象，不得写入论文。修复方案见 §12 第 7 条。
- 2026-09-12：问题 2–4 的强退化水分扩散系数正式采用 Kirchhoff 积分平均；问题 3、4 的物理阈值均为严格 \(C<0.15\)，工作簿导出用原始值 \(C<0.14995\) 保证四位小数显示合规。
- 2026-09-12：问题 1 已形成全项目论文图视觉母版：包含四面板 `q1_overview`、圆柱横截面演化小多图、温度-含水率状态轨迹和双场三维地形，固定中心/内部/表面的语义色与线型；水分图统一显示实际干基含水率 \(C\)。增强版独立写入 `outputs/q1/figures/fancy/` 并统一输出 PNG、矢量 PDF 和 SVG，不覆盖根目录经典版；问题 2–4 后续复用 `src/visualization_style.py`。
- 2026-09-12：新增 Q3 增强展示图入口和 8 张三格式预览图，复用宋体/Times New Roman 论文母版与 `YlGnBu` 含水率色标；由于未舍入 Q3 数据尚未入库，当前只读四位小数交付表，并在阈值相关图中显式标注 `0.1500` 不能证明严格不等式。正式图仍须由 Kirchhoff 修正结果重生。
- 2026-09-12：问题 2 求解器并入 `src/q2_coupled.py`。相对项目外原型的改动限于：附件与输出路径改为项目结构；函数与参数改名以对齐 `q3_drying_time.py` 既有的 import（`rho_f/cp_f/k_f/D_f/assemble/bsolve`、`P.T0/P.C0`）；删除潜热分支（原型中 `eta` 默认 0、`qlat` 恒为 0，删除为数值恒等，且与 §4.3 的无潜热口径一致）；Picard 达迭代上限时告警；输出时刻改为定点匹配以免 \(\Delta t\neq1\) 时静默丢输出。离散格式、时间推进、界面平均方式与物性公式未动。
- 2026-09-12：对拍确认项目外原型与仓库结果一致，不存在未记录的数值差异。问题 1 原型（稀疏 `splu` + Crank–Nicolson）与 `outputs/q1/data/q1_results_full.npz` 在全部 1800×21 个点上的最大偏差为 \(7.6\times10^{-12}\ ^\circ\mathrm C\) 与 \(1.1\times10^{-14}\ \mathrm{kg/kg}\)，属舍入量级；入库后的问题 3 重新给出 \(t_{\text{end}}=207120\ \mathrm s=57.533\ \mathrm h\)、网格收敛 58.983/57.717/57.533 h、Richardson 外推 57.472 h，与 §10 记录一致，逐点值与 `result3.xlsx` 的四位小数零偏差。
- 2026-09-12：确认本机验证环境为 Python 3.14 framework 解释器（见 §9），依赖版本仍未锁定。
- 2026-09-12：新增 Q2 增强展示图入口和 8 张三格式图，复用全项目宋体/Times New Roman 母版、反向 `magma` 温度色标与 `YlGnBu` 含水率色标；除双场历史、时空图、剖面和截面外，增加变物性演化、耦合轨迹及 Q1/Q2 前 30 min 同尺度对比。当前图读取未舍入 NPY，但仍基于 Kirchhoff 修正前求解结果，正式定稿须重生。
- 2026-09-12：问题 4 完成正式移动边界求解、未舍入数据和 `result4.xlsx` 导出；连续终止时间 51.104 h，同附录 4 物性的固定半径对照为 129.851 h，收缩使时长缩短 60.6%。结构化输出改用忽略大小写后仍唯一的文件名，避免 APFS 上 `Q4_R/Q4_r`、`Q4_full_t/Q4_full_T` 互相覆盖。
- 2026-09-12：新增 Q4 增强展示图入口和 8 张三格式图；物理域热图在移动表面外留白，圆截面按统一初始尺度绘制以真实呈现缩小，固定/移动半径比较只使用相同附录 4 物性和 Kirchhoff 离散。固定半径对照缓存为 `outputs/q4/data/q4_fixed_control.npz`，求解器或参数变化后必须刷新。

### 13.2 已废弃或被替代的方案

- “问题 1、2 以表面潜热模型为主、无潜热作对照”已废弃：该方案把归一化含水率通量乘估算 \(\rho_d\) 后当作真实质量通量，但题面未给出绝对储水标度，且固体干基含水率与空气含湿量的基准不同；数值上还产生显著过冷和强参数敏感性。
- “问题 2 先计算问题 1，再在 1800 s 切换附件 3 物性”未采用：当前从 \(t=0\) 统一使用附件 3，避免人为物性跳变。
- “用点值调和平均得到低湿度下不可穿透表层，并据此宣称存在最优环境湿度或真实结壳停滞”已废弃：独立复核表明这是强退化扩散系数与离散方式共同造成的数值假象，正式结果改用 Kirchhoff 积分平均并做网格收敛验证。
- 不采用温度裁剪、水分裁剪或任意限幅来制造看似合理的结果；出现异常应回到物理闭合、单位、边界和离散过程排查。
