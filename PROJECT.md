# CUMCM 2026 A 题：药材烘干建模项目

> 本文档是项目级长期记忆。它记录当前有效的研究目标、模型假设、数值路线、运行方式、验收标准和关键决策；临时推导、完整结果表和调试过程应放在专题文档、代码或输出目录中。

## 1. 项目概况与当前状态

- 比赛：2026 年高教社杯全国大学生数学建模竞赛。
- 当前主线：暂定 A 题“药材的烘干问题”；C 题仅保留为发生重大物理或数值障碍时的回退方向，B 题不在当前工作范围内。
- 当前日期：2026-09-12（Asia/Shanghai）。
- 当前阶段：问题 1 已形成“输入校验—求解—数值验证—结构化输出—官方模板导出—论文图生成”的可重复运行代码；问题 2 的模型推导已经完成，尚未编程；问题 3、4 尚未展开。
- 当前结论：问题 1 求解器在离散守恒、极限测试和网格收敛方面表现良好，`result1.xlsx` 可由程序自动重建。问题 1 只保留内部导热与表面对流换热模型，不再实现或计算潜热模型。

## 2. 研究背景与物理分析目标

研究对象是一根近似圆柱形的药材。烘干过程中，热量由烘房空气经表面对流进入药材，水分在药材内部扩散并从表面进入空气。项目采用题面参数能够闭合的简化模型，目标是描述温度场和干基含水率场随时间、径向位置的变化，并形成可复现的数值结果和论文证据链。

四个问题的目标依次为：

1. **问题 1：短时常物性过程。** 计算前 1800 s 内固定圆柱的径向温度和含水率分布，建立常物性模型和数值验证基线。
2. **问题 2：固定尺寸变物性过程。** 使用附件 3 的经验物性，从原始初态重新计算完整热–湿耦合过程，并按要求输出前 3 h 的温度和含水率。
3. **问题 3：烘干终止时间。** 在固定尺寸模型下，求药材全域含水率均低于 \(0.15\ \mathrm{kg/kg}\) 的最早时刻。
4. **问题 4：收缩影响。** 根据附件 2 的半径变化建立移动边界模型，比较收缩对温度、水分迁移和烘干时间的影响。

## 3. 官方材料与数据

官方文件均位于 `data/official/A题/`，应视为只读输入，不得在原文件上修改。`data/official/` 同时保留其他题目的原始材料和比赛格式文件，作为选题档案。

- `A题.pdf`：题目正文。
- `附件/附件1.xlsx`：241 个时刻，时间范围 0–14400 s，间隔 60 s；包含烘房温度和烘房水分浓度，无缺失。
- `附件/附件2.xlsx`：145 个时刻，时间范围 0–259200 s，间隔 1800 s；药材半径由 2 cm 下降到约 1.198 cm，无缺失。
- `附件/附件3/result1.xlsx` 至 `result4.xlsx`：问题 1–4 的官方结果模板，必须由程序写入，避免手工复制。

当前数据解释：

- 附件 1 的离散边界数据在采样点之间采用分段线性插值。
- 问题 2 的结果只到 10800 s，处于附件 1 范围内，不需要外推。
- 问题 3 的计算会超过 14400 s，长期烘房温度和水分边界的延拓规则尚未确定。
- 烘房水分浓度和药材干基含水率虽然都以 kg/kg 表示，但分母物理含义不同；当前把前者视为可用于题面边界式的等效外部浓度，这是一项待校准的闭合假设。

## 4. 当前基本假设

### 4.1 通用假设

1. 药材近似为长 \(L=0.25\ \mathrm m\)、初始半径 \(R=0.02\ \mathrm m\) 的圆柱。
2. 药材和环境关于中心轴对称，主要梯度沿径向；当前忽略轴向传热传质和两个端面的影响。
3. 药材视为均匀、各向同性的有效连续介质。
4. 内部传热采用傅里叶导热，内部水分迁移采用有效 Fick 扩散。
5. 圆柱中心采用零通量对称边界；表面采用第三类换热和传质边界。
6. 水分被等效为在药材内部迁移、在表面离开；题面给出的归一化含水率通量不直接解释为 kg/(m²·s) 的真实蒸发质量通量。
7. 问题 1 和问题 2 均不考虑蒸发潜热，也不保留含潜热计算分支；问题 3 的口径在实现前另行决定。
8. 暂不考虑热辐射、化学反应热、内部宏观流动和多相局部非平衡。

### 4.2 问题 1

- 固定半径，不考虑收缩。
- \(\rho=820\ \mathrm{kg/m^3}\)、\(c_p=2600\ \mathrm{J/(kg\,K)}\)、\(k=0.36\ \mathrm{W/(m\,K)}\)。
- \(h=25\ \mathrm{W/(m^2\,K)}\)、\(h_m=8\times10^{-7}\ \mathrm{m/s}\)。
- \(T(r,0)=28\ ^\circ\mathrm C\)，\(C(r,0)=2.55\ \mathrm{kg/kg}\)。
- \(D(C)=7\times10^{-9}\exp(-0.89/C)\ \mathrm{m^2/s}\)。
- 温度场只考虑内部导热和表面对流换热，与水分场独立求解。

### 4.3 问题 2 与问题 3

- 问题 2 从 \(t=0\) 的原始初态重新求解，不先运行问题 1 再在 1800 s 切换物性。
- 整个计算时域统一采用附件 3 的 \(\rho(C)\)、\(c_p(C)\)、\(k(C)\) 和 \(D(C,T_K)\)，其中扩散系数中的温度必须使用 K。
- 问题 2、3 暂不考虑收缩；问题 4 才引入时变半径。
- 问题 2 不考虑蒸发潜热，不估算干物质体积密度，也不建立含潜热敏感性对照。

## 5. 当前模型与关键接口

### 5.1 内部控制方程

温度采用轴对称圆柱中的守恒形式：

\[
\rho(C)c_p(C)\frac{\partial T}{\partial t}
=\frac1r\frac{\partial}{\partial r}
\left[rk(C)\frac{\partial T}{\partial r}\right].
\]

问题 1 中 \(\rho,c_p,k\) 为常数；问题 2、3 中它们随局部含水率变化。

水分采用

\[
\frac{\partial C}{\partial t}
=\frac1r\frac{\partial}{\partial r}
\left[rD(C,T_K)\frac{\partial C}{\partial r}\right].
\]

问题 1 中 \(D=D(C)\)，问题 2、3 中 \(D=D(C,T_K)\)。变系数 \(k\) 和 \(D\) 必须保留在散度算子内部。

### 5.2 表面第三类边界

题面形式的归一化传质边界为

\[
-D_s\left.\frac{\partial C}{\partial r}\right|_{r=R}
=h_m(C_s-C_\infty).
\]

该式两边单位均为 m/s，只定义以含水率变量表示的归一化通量。问题 1 不把它换算为额外的能量通量。温度采用题面给出的第三类换热边界：

\[
-k_s\left.\frac{\partial T}{\partial r}\right|_{r=R}
=h(T_s-T_\infty).
\]

### 5.3 耦合关系

- 问题 1：\(D\) 不依赖温度，热物性也为常数，因此正式温度场与水分场独立求解。
- 问题 2、3：温度通过 \(D(C,T_K)\) 影响水分，含水率通过 \(\rho(C),c_p(C),k(C)\) 影响温度，即使不加入潜热仍属于双向非线性耦合，必须同步或迭代求解。

## 6. 数值方法与实现原则

### 6.1 当前问题 1 求解器

`src/q1_fvm.py` 当前采用：

- 一维径向有限体积离散，中心和表面使用半控制体积；
- 界面扩散系数使用调和平均；
- Crank–Nicolson 型 \(\theta=0.5\) 时间推进，并用 Rannacher 启动抑制初始边界突变引起的振荡；
- 水分非线性方程使用 Picard 迭代；
- 分别求解温度场和水分场，只生成一套正式结果；
- 用恒定环境温度下的 Bessel 解析解检查热传导离散；
- 用局部/累计守恒残差和三组网格时间设置检查结果。
- 线性三对角方程使用内置 Thomas 分解，不再依赖 SciPy 稀疏求解器；SciPy 仅用于可选的 Bessel 解析基准。
- 对附件 1 的表头、数值完整性、时间单调性和时域覆盖进行运行前校验。
- 自动从官方模板副本生成 `outputs/q1/workbooks/result1.xlsx`，并回读检查两个工作表的尺寸、边界坐标和全部结果单元格。

当前基准网格为 \(N=400\)、\(\Delta t=0.5\ \mathrm s\)；收敛比较使用 \((N,\Delta t)=(200,1.0),(400,0.5),(800,0.25)\)。这些是当前实现参数，不是不可更改的物理设定。

### 6.2 后续问题

- 问题 2：继续采用守恒有限体积和隐式时间推进，在每个时间层内用 Picard 交替更新 \(C\)、\(D\)、\(\rho,c_p,k\) 和 \(T\)；不计算表面潜热通量。
- 问题 3：复用问题 2 求解器，采用事件检测确定 \(\max_r C(r,t)\le0.15\) 的最早时刻；必须先确定附件 1 范围外的环境边界。
- 问题 4：将 \(r\in[0,R(t)]\) 映射到固定坐标，显式处理网格运动引入的输运项，并重新检查水分质量守恒。
- 内部计算网格和时间步不得由结果模板的输出间隔反向决定；结果应在求解后插值或采样到官方网格。

## 7. 工作流程与当前优先级

1. **冻结问题 1 模型。** 采用对流换热温度模型与独立水分扩散模型，固定参数来源、正负号和单位。
2. **生成问题 1 论文图。** 正式工作簿和论文表格已自动化，下一步生成定稿图并说明模型简化。
3. **实现问题 2。** 抽取可复用的数据、网格、边界和导出模块，实现变物性双向耦合求解器。
4. **完成问题 3。** 明确长期环境边界和终止判据，完成数日积分及步长/网格验证。
5. **完成问题 4。** 建立收缩坐标模型，与固定半径模型做对照和守恒分析。
6. **论文交付。** 固定图表和消融实验后撰写正文，确保每个关键数字都可由代码复现。

## 8. 当前文件与计划代码结构

### 8.1 当前有效文件

```text
PROJECT.md                                  # 项目级长期记忆
requirements.txt                           # 当前直接依赖，版本待验证后锁定
data/official/                              # 全部官方题目、附件和格式文件，只读
src/q1_fvm.py                               # 问题 1 完整求解、验证和导出入口
src/q1_visualize.py                         # 问题 1 论文图生成入口
tests/test_q1_fvm.py                        # 问题 1 快速回归与极限测试
docs/q1/heat_conduction.md                  # 问题 1 热传导
docs/q1/moisture_diffusion.md               # 问题 1 水分扩散
docs/q1/heat_equation_derivation.md         # 热方程补充推导
docs/q2/coupled_heat_moisture_derivation.md # 问题 2 推导源文档
outputs/q1/data/                            # 问题 1 CSV、NPZ 和 JSON 数值结果与诊断
outputs/q1/workbooks/result1.xlsx           # 由官方模板自动生成的完整结果
outputs/q1/figures/                         # 问题 1 PNG、PDF 等论文图
tmp/                                       # PDF 页面、缓存等临时文件
```

`__pycache__/`、`.DS_Store` 和 `tmp/` 中的内容不是研究成果。旧版草稿、独立潜热输出和过时的派生文档已从项目目录清理；当前可复现工作簿统一为 `outputs/q1/workbooks/result1.xlsx`。

### 8.2 目标结构

在继续开发时逐步整理为：

```text
src/        # 数据读取、物性、网格、模型和求解器
scripts/    # 问题 1–4 的一键运行入口
tests/      # 单元、解析解、守恒、极限和回归测试
outputs/    # 可由代码重建的结果、图表和工作簿
paper/      # 论文正文及定稿图表
logs/       # 参数、运行信息和收敛记录
```

不要为了目录整齐提前创建空结构；在问题 2 实现时再从 `src/q1_fvm.py` 抽取公共模块。

## 9. 运行流程

当前问题 1 主求解与 Excel 导出需要 Python、NumPy 和 OpenPyXL；SciPy 只用于恒温 Bessel 解析基准，缺失时会明确跳过该项而不影响主求解。`requirements.txt` 列出完整验证环境所需依赖，版本尚未锁定；应在竞赛使用环境验证通过后固定版本。

从项目根目录安装依赖并运行：

```bash
python -m pip install -r requirements.txt
python src/q1_fvm.py
```

程序默认会：

1. 读取 `data/official/A题/附件/附件1.xlsx`；
2. 运行恒温 Bessel 解析解对照；
3. 运行三组网格与时间步收敛检查；
4. 分别求解问题 1 的温度场和水分场；
5. 写出 `outputs/q1/data/q1_results_full.csv`、`q1_results_full.npz` 和 `q1_summary.json`；
6. 从官方模板副本生成 `outputs/q1/workbooks/result1.xlsx`，写入 1–1800 s、0–2 cm 每隔 0.1 cm 的完整结果并回读验收。

仅调试代码时可加 `--quick` 跳过网格加密和解析基准。快速回归测试命令为：

```bash
python -m unittest discover -s tests -v
```

生成问题 1 的默认 PNG 图：

```bash
python src/q1_visualize.py
```

同时生成 PNG 和适合论文排版的 PDF：

```bash
python src/q1_visualize.py --formats png pdf
```

默认输出温度时间曲线、含水率时间曲线、温度/含水率时空热图，以及不同时刻的径向剖面图。时间曲线默认比较 \(r=0,0.5,1.0,1.5,2.0\ \mathrm{cm}\)，可通过 `--radii` 调整。

## 10. 当前结果及其解释边界

最近一次计算得到的主要结果和诊断为：

- 热方程累计相对守恒残差约 \(1.3\times10^{-14}\)；
- 水分累计相对守恒残差约 \(6.5\times10^{-14}\)；
- 1800 s 中心/表面温度约为 \(33.58/36.79\ ^\circ\mathrm C\)，与预热升温过程一致；
- 1800 s 中心/表面含水率约为 \(2.5500/1.5102\ \mathrm{kg/kg}\)。

代码结构调整后，旧版 `q1_results_full.csv`、`q1_results_full.npz` 和 `q1_summary.json` 已移出项目；重新运行程序后会按单一温度字段的新格式生成。

## 11. 验收标准

### 11.1 输入与复现

- 官方原始文件不被修改；生成文件全部进入独立输出目录。
- 数据点数、时间范围、单位和缺失值检查通过。
- 依赖、参数、运行入口和输出路径明确；全新环境能够重建论文使用的所有数字、图和工作簿。
- 内部计算不提前四舍五入，最终结果按题目要求保留位数。

### 11.2 物理与量纲

- 温度、含水率、热通量和归一化水分通量的单位一致。
- 中心热通量和水分通量为零；表面第三类边界正负号正确。
- 归一化水分库存变化与累计表面归一化流失量闭合。
- 正式热方程满足“储热项 = 累计对流输入”的离散平衡；变物性模型不得直接套用问题 1 的常物性总显热公式。
- 状态量不出现无物理依据的负值、过冲或振荡；附件 1 升温阶段的正式温度结果不应无依据地降到初温以下。

### 11.3 数值可信度

- 至少完成两组以上空间网格和时间精度的收敛比较。
- 常物性热方程通过 Bessel 解析解或另一独立方法交叉验证。
- 非线性迭代残差、局部守恒残差和累计守恒残差达到预定阈值。
- 极限检查通过：\(h_m=0\) 时归一化水分通量为零；\(h=h_m=0\) 时表面绝热且不失水。
- 正式结果检查换热/传质系数和关键边界解释。

### 11.4 各问交付

- 问题 1、2 的指定时间和径向位置结果稳定复现，并自动写入官方模板。
- 问题 3 使用全域判据 \(\max_r C(r,t)\le0.15\)，终止时间对网格和时间精度稳定。
- 问题 4 明确空间坐标、材料坐标、表面位置和网格速度，移动边界下水分质量守恒。
- 论文中的每个关键数字均能追溯到固定参数、代码版本和输出文件。

## 12. 主要风险与待决策事项

1. **界面含水率基准。** 固体干基含水率和空气含湿量不能天然直接作差；当前按题面形式把后者视为等效外部状态量，这是水分模型需要在论文中声明的主要简化。
2. **变物性能量解释。** \(\rho(C)c_p(C)\partial T/\partial t\) 是工程表观热容模型；若需要总焓模型，必须有额外物理依据，不能随意增加未知参数。
3. **长期环境边界。** 问题 3 超出附件 1 的 4 h 数据范围，恒温阶段的 \(T_\infty\) 和 \(C_\infty\) 延拓规则尚未冻结。
4. **端面效应。** 当前一维径向简化需要用长径比、尺度分析或敏感性说明合理性。
5. **收缩模型。** 问题 4 的固定坐标映射、材料运动和质量守恒尚未推导。
6. **工程复现。** 问题 1 已实现一键求解、结构化诊断和 Excel 导出，但依赖版本仍未锁定；Bessel 解析基准需要可选 SciPy，缺失时报告为跳过。问题 2 开发时仍需抽取通用模块，避免继续扩大单文件。

## 13. 设计决策与已废弃方案

### 13.1 当前有效决策

- 2026-09-10：团队暂定 A 题为主线；C 题保留为重大障碍下的回退方案。
- 2026-09-11：问题 1 采用固定圆柱的一维轴对称径向模型，温度场与水分场独立求解。
- 2026-09-11：问题 2 从原始初态重新求解，并在整个时域使用附件 3 变物性；模型通过 \(D(C,T_K)\) 和含水率相关热物性形成双向耦合，不考虑表面蒸发潜热。
- 2026-09-11：问题 1、2 的正式温度模型不考虑潜热；问题 3 的口径在实现前另行冻结。
- 2026-09-11：空间离散优先使用守恒有限体积法，时间推进优先使用隐式方法；问题 1 原型采用 Crank–Nicolson、Rannacher 启动和 Picard 迭代。
- 2026-09-11：项目文件按 `data/`、`src/`、分题文档和分题输出重构；官方材料集中只读保存，源文档与派生展示文件分离，数值数据与工作簿分开归档。
- 2026-09-11：新增最小 `requirements.txt` 声明直接依赖；由于当前主机没有通过验证的完整 Python 环境，依赖版本暂不锁定，正式运行环境建立后再固定。
- 2026-09-11：问题 1 完成正式运行入口、官方 `result1.xlsx` 自动导出、回读验收和标准库单元测试；主三对角求解改为内置 Thomas 算法，SciPy 降为解析基准的可选依赖。
- 2026-09-11：根据量纲、变量基准和可识别性复核，将 `result1.xlsx` 导出确定为不含潜热的温度结果。
- 2026-09-12：冻结问题 2 的无潜热口径：能量方程和表面热边界均不含潜热项，不估算 \(\rho_d\)、\(L_v\) 或真实蒸发质量通量，也不建立含潜热对照模型。
- 2026-09-12：清理不再使用的独立潜热数据、旧潜热工作簿、旧原始草稿、过时 Q2 派生 HTML 和已完成使命的外部方案评审；保留 Q1 正式结果、综合诊断以及解释排除潜热所需的论证。
- 2026-09-12：进一步精简问题 1 求解器，删除潜热参数、通量接口、敏感性循环、模型选择参数及相关输出字段；问题 1 只生成一套温度结果。
- 2026-09-12：新增独立的 Q1 可视化入口，从 NPZ 结果生成位置时间曲线、双场时空热图和径向剖面图；默认输出 PNG，并支持 PDF、SVG。

### 13.2 已废弃或被替代的方案

- “问题 1、2 以表面潜热模型为主、无潜热作对照”已废弃：该方案把归一化含水率通量乘估算 \(\rho_d\) 后当作真实质量通量，但题面未给出绝对储水标度，且固体干基含水率与空气含湿量的基准不同；数值上还产生显著过冷和强参数敏感性。
- “问题 2 先计算问题 1，再在 1800 s 切换附件 3 物性”未采用：当前从 \(t=0\) 统一使用附件 3，避免人为物性跳变。
- 不采用温度裁剪、水分裁剪或任意限幅来制造看似合理的结果；出现异常应回到物理闭合、单位、边界和离散过程排查。
